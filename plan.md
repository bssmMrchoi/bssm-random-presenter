# plan.md — 랜덤발표자뽑기 기능 명세

수정 요구사항을 요청할 때 이 파일을 기준으로 한다.
모든 소스 코드는 `src/pages/Index.tsx` 한 파일에 집중되어 있다.

---

## 0. 투수 캐릭터 (SVG)

`giants.png` (롯데 자이언츠 오리 마스코트)를 최대한 닮은 SVG 투수 캐릭터로 교체한다.

### 참고 이미지 특징

- **종류**: 오리 캐릭터 (흰 몸통, 주황색 부리)
- **체형**: 머리가 크고 몸이 작은 치비(chibi) 비율
- **유니폼**: 네이비+흰색, 상의에 "Giants" 또는 "G" 로고
- **모자**: 네이비 야구 모자, 흰 챙, 정면에 "G" 마크
- **발**: 주황색 오리 발 (신발 아님)
- **눈**: 크고 동그란 눈
- **날개/팔**: 짧고 통통한 날개형 팔

### 게임 내 포즈 (투구 동작)

| Phase | 포즈 |
|-------|------|
| `idle` | 기본 직립 자세 |
| `windup` | 투구 팔(날개) 들어올림, 몸 약간 뒤로 기울기 |
| `pitch` | 팔(날개) 앞으로 뻗음, 몸 앞으로 기울기 |

### SVG 구현 요소

- 흰색 타원 몸통
- 주황색 부리 (납작한 오리 부리)
- 네이비 야구 모자 (G 텍스트)
- 큰 원형 눈 (흰자 + 검은 동공 + 하이라이트)
- 네이비+흰색 유니폼 (Giants 로고)
- 주황색 오리 발 2개
- 짧고 통통한 날개형 팔 (투구 동작에 따라 위치 변환)

---

## 1. 학생 명단

- 기본값: `"곽영빈, 김민서, 김주연, 김효주, 노현승, 박건, 박시후, 윤동언, 이석찬, 이우린, 전지원, 조현우, 최수혁"` (13명)
- 파일 상단 `MEMBERS` 배열은 기본값 소스로만 사용 (하드코딩 유지)

### 명단 편집 기능 (UI)

- idle 상태에서 명단을 직접 수정할 수 있는 텍스트 입력창 제공
- 입력 형식: `,`로 구분된 이름 문자열 (예: `홍길동, 김철수, 이영희`)
- 공백은 자동으로 trim 처리
- 입력 후 적용(확인) 시:
  - 기존 `weights`, `currentWeights` 초기화 → 새 명단 기준으로 전원 가중치 1로 재생성
  - 가중치 설정 패널이 새 인원에 맞게 재구성됨
- 빈 항목(연속 쉼표, 공백만 있는 항목)은 무시
- 최소 인원 제한: 1명 이상이어야 적용

### 셔플 버튼

- 명단 편집창에 **[🔀 셔플]** 버튼 추가 (적용 버튼 옆에 배치)
- 클릭 시: 현재 입력창에 있는 이름 목록을 Fisher-Yates 방식으로 무작위 재정렬하여 입력창에 반영
- 적용(확인)은 하지 않음 — 셔플 후 사용자가 직접 **[적용]** 버튼을 눌러야 실제 명단에 반영됨
- 입력창이 비어 있거나 이름이 1개뿐이면 동작 없음 (버튼 비활성화 또는 무시)
- 기존 `applyMembers` 로직에는 영향 없음 — `memberInput` state 값만 변경

---

## 2. 게임 흐름 (투구 사이클)

버튼을 누를 때마다 아래 순서로 진행된다.

```
[투구! 버튼 클릭]
  → currentWeights 기반으로 학생 1명 선택 (임시)
  → 투수 와인드업 애니메이션 (1.2초)
  → 공 날아가는 애니메이션 (0.8초)
  → 선택된 학생 이름 표시 + "판정 대기 중..." (1.2초)
  → 스트라이크 / 볼 판정
       스트라이크 → 해당 학생 currentWeights -1 / strikeCount +1
           strikeCount == strikeTarget → 발표자 확정 (reveal)
           아직 부족 → strike-wait (1.5초 표시 후 자동으로 다음 투구)
       볼 → 해당 학생 currentWeights -1 / ballCount +1
           ballCount == 4 → FOUR BALL 면제
           아직 부족 → ball-wait (1.5초 표시 후 자동으로 다음 투구)
  → 발표자가 나올때까지 자동 반복 (버튼 입력 불필요)
```

### 학생 선택 알고리즘 요건

- **가중치 시스템 유지**: `currentWeights` 기반 가중 확률 선택 방식은 그대로 유지
- **명단 순서 편향 제거**: `Object.entries()` 는 항상 동일한 삽입 순서로 반환되므로, CDF 선택 전에 **Fisher-Yates 셔플**로 항목 순서를 무작위화
- 구현: 항목 배열 셔플 → 누적 확률(CDF) 방식으로 최종 선택

---

## 3. 스트라이크 / 볼 확률

투구 횟수(볼+스트라이크 합산)에 따라 스트라이크 확률이 증가한다.

| 투구 횟수 | 스트라이크 확률 | 볼 확률 |
|-----------|----------------|---------|
| 1번째     | 10%            | 90%     |
| 2번째     | 30%            | 70%     |
| 3번째     | 50%            | 50%     |
| 4번째 이상 | 70%           | 30%     |

---

## 4. 카운트 표시 (B / S)

화면 상단에 야구 카운트 표시:

- **B (볼)** — 초록 동그라미 4개 고정. 볼이 쌓일수록 왼쪽부터 채워짐
- **S (스트라이크)** — 빨간 동그라미 3개 고정. 스트라이크가 쌓일수록 왼쪽부터 채워짐

---

## 5. 스트라이크 목표 설정 (몇 번째 스트라이크에 아웃?)

idle 상태에서만 표시. 게임 시작 전 설정 가능.

- **1S / 2S / 3S** 버튼 — 몇 번째 스트라이크에 발표자가 확정될지 선택
  - **직접 클릭 시**: 해당 버튼이 강조(filled) 표시됨. 🎲 랜덤 버튼은 비활성(outline) 상태로 전환
  - **🎲 랜덤 모드일 때**: 1S/2S/3S 버튼 모두 강조 없이 outline 상태 유지. 내부적으로는 값이 설정되어 있지만 어떤 값인지 버튼 UI에 드러나지 않음
  - 기본값: 3S (직접 선택 상태)
- **🎲 랜덤** 버튼 — 1~3 중 무작위로 내부 값 설정
  - **클릭 시**: 🎲 랜덤 버튼 자체가 강조(filled/선택됨) 상태로 표시됨
  - 1S/2S/3S 버튼은 모두 outline 상태로 유지 (어떤 값이 선택됐는지 드러나지 않음)
  - 구현: `isRandomMode` 플래그로 "직접 선택 vs 랜덤 선택" 구분. `isRandomMode === true`일 때 🎲 버튼 강조

---

## 6. 가중치 개념 및 모수 관리

### 가중치 정의

**가중치 = 해당 학생이 모수에 들어가는 복사본 수**

- 가중치 1: 해당 학생 이름 1개 → 전원 가중치 1이면 총 13명 모수
- 가중치 2: 해당 학생 이름 2개 → 이우린 가중치 2이면 총 14명 모수
- 선택 확률 = 해당 학생 가중치 / 전체 가중치 합
- 구현: 누적 확률 분포(CDF) 방식

### 투구 후 모수 감소 (항상 적용)

투구 결과(볼 또는 스트라이크)가 나온 학생은 **현재 모수(`currentWeights`)에서 1 차감**된다.

| 상황 | 결과 |
|------|------|
| 해당 학생 currentWeights = 1 | 차감 후 0 → 다음 투구부터 모수에서 완전 제외 |
| 해당 학생 currentWeights = 2 이상 | 차감 후 1 이상 → 다음 투구에서 1개 적은 확률로 잔류 |

예시: 이우린 가중치 3 설정 → 첫 번째 결과 후 currentWeights=2, 두 번째 결과 후 =1, 세 번째 결과 후 =0 (제외)

### 상태 분리

| 변수 | 설명 |
|------|------|
| `weights` | 사용자가 슬라이더로 설정한 값. 리셋해도 유지됨 |
| `currentWeights` | 게임 중 실제 모수. 투구마다 차감됨. 리셋 시 `weights`로 복원 |

### 모수 소진 시

모든 학생의 `currentWeights`가 0이 되면 `currentWeights`를 `weights` 값으로 복원 후 다시 선택

---

## 7. 가중치 설정 패널

idle 상태에서만 표시.

- **"⚖️ 가중치 보기 / 숨기기"** 버튼으로 토글
- 카드 형태, 학생별 슬라이더(1~5) + 현재 배율(`x1`~`x5`) 표시
- 그리드 레이아웃: 모바일 1열, sm 이상 2열
- 투구 시작 시 자동으로 숨겨짐

---

## 8. 게임 상태(Phase) 목록

| Phase | 설명 |
|-------|------|
| `idle` | 대기 중. 버튼/설정 모두 표시 |
| `windup` | 투수 와인드업 (1.2초) |
| `pitch` | 공 날아가는 중 (0.8초) + 스피드 라인 |
| `name-show` | 선택된 이름 + "판정 대기 중..." 표시 |
| `strike` | "STRIKE!" 텍스트 + 화면 흔들림. 최종 스트라이크면 → `reveal`. 아직 부족하면 → `strike-wait` |
| `strike-wait` | 발표자 미확정 스트라이크 후 결과 표시 (1.5초). 이름 취소선만 표시. 1.5초 후 자동으로 다음 투구 시작 |
| `ball` | "BALL!" 텍스트 + 이름 취소선 표시 |
| `ball-wait` | 볼 판정 후 결과 표시 (1.5초). "BALL!" 유지. 1.5초 후 자동으로 다음 투구 시작 |
| `reveal` | 발표자 확정. 이름 + "오늘의 발표자 확정!" |
| `fourball` | 4볼 면제. "FOUR BALL!" + 이름 + "발표 패스!" |

---

## 9. 화면 구성 (전체 레이아웃)

전체 화면은 **좌우 2열** 구조로 변경한다.

```
┌─────────────────┬──────────────────────────────────────┐
│  학생 명단 패널  │           메인 콘텐츠 영역            │
│  (좌측 고정)    │  ① 제목                              │
│                 │  ② 카운트 표시: B ○○○○ / S ○○○    │
│  가중치만큼     │  ③ 메인 스테이지 (투수, 공, 텍스트)  │
│  중복 표시      │  ④ 버튼 영역                         │
│  취소선 표시    │  ⑤ 스트라이크 목표 선택 (idle만)     │
│                 │  ⑥ 명단 편집창 (idle만)              │
│                 │  ⑦ 가중치 설정 패널 (idle만)         │
└─────────────────┴──────────────────────────────────────┘
```

### 좌측 학생 명단 패널 (항상 표시)

- 게임 진행 중 항상 표시 (phase 무관)
- **가중치(`currentWeights`) 기반 항목 반복 표시**: 가중치만큼 이름을 반복해서 나열
  - 예: 이우린 가중치 3 → `이우린 / 이우린 / 이우린` (3개)
  - 예: 박건 가중치 1 → `박건` (1개)
  - 전체 가중치 합 = 패널에 표시되는 이름 항목 수 → 실제 추첨 모수와 1:1 대응
- 정렬: 학생 이름 **오름차순(가나다 순)** 기준, 동일 학생의 중복 항목은 연속 배치
- `currentWeights` 가 변경될 때마다 패널도 실시간 반영 (투구마다 1씩 차감되는 모수 시각화)
- 각 항목(행)의 표시 상태:

| 상태 | 표시 방식 |
|------|-----------|
| 아직 선택된 적 없는 항목 | 기본 텍스트 |
| 볼 또는 비확정 스트라이크로 소진된 항목 | ~~취소선~~ + 흐린 색상 |
| 발표자 확정 (reveal) — 해당 학생 전체 항목 | 강조 색상 + 볼드 (취소선 없음) |
| 4볼 면제 (fourball) — 해당 학생 전체 항목 | 초록 색상 + 볼드 (취소선 없음) |

- 취소선 트리거: `ball` 또는 `strike-wait` phase 진입 시 해당 `tempName`의 항목 1개 소진(취소선)
  - 소진 순서: 해당 학생의 첫 번째 살아있는 항목부터 순서대로
- 누적 관리: 한 게임 내 취소선은 쌓여서 유지됨
- 리셋(`fullReset`) 시 모든 취소선 + 항목 수 초기화 (`weights` 기준으로 복원)

### 메인 콘텐츠 영역 (위→아래)

```
① 제목: "⚾ 랜덤 발표자 뽑기"
② 카운트 표시: B ○○○○ / S ○○○
③ 메인 스테이지 (투수 SVG, 공, 텍스트 오버레이)
④ 버튼 영역
   - idle: [⚾ 투구!]  [⚖️ 가중치]
   - ball-wait/strike-wait: 버튼 없음 (자동으로 다음 투구 진행)
   - reveal/fourball: [🔄 처음부터]
⑤ 스트라이크 목표 선택 (idle만): [1S] [2S] [3S] [🎲 랜덤]
     - 1S/2S/3S 직접 선택: 해당 버튼 filled, 나머지 outline, 🎲 버튼 outline
     - 🎲 랜덤 선택: 🎲 버튼 filled, 1S/2S/3S 모두 outline (어떤 값인지 숨김)
⑥ 명단 편집창 (idle만): 텍스트 입력 + 적용 버튼
⑦ 가중치 설정 패널 (idle + showWeights)
```

---

## 10. 애니메이션 / 시각 효과

| 효과 | 트리거 | 설명 |
|------|--------|------|
| 투수 와인드업 | `windup` phase | 날개(팔) 들어올림, 몸 뒤로 기울기 |
| 공 날아가기 | `pitch` phase | `ball-fly` 키프레임, 0.7초 |
| 공 빗나가기 | `ball` phase | `ball-miss` 키프레임, 오른쪽으로 사라짐 |
| 스피드 라인 | `pitch` phase | 8개 선이 flash 애니메이션 |
| STRIKE! 텍스트 | `strike`/`reveal` | `strike-slam` 키프레임, 빨간색 |
| BALL! 텍스트 | `ball`/`ball-wait` | `strike-slam` 키프레임, 초록색 |
| 이름 등장 | `name-show` | `winner-pop` 키프레임 (팝 효과) |
| 화면 흔들림 | `strike`/`fourball` | `shake` 키프레임, 0.4초 |
| 발표자 확정 | `reveal` | 이름 크게 팝업, primary 색상 |
| FOUR BALL | `fourball` | 파란 텍스트 + 이름 + 초록 서브텍스트 |
| 배경 빛 효과 | 애니메이션 중 | 노란 블러 원 2개 pulse |

---

## 11. 리셋

- **🔄 처음부터** 버튼: `reveal` / `fourball` phase에서만 표시
- 볼/스트라이크 카운트, winner, tempName 모두 초기화
- `currentWeights` → `weights` 값으로 복원 (모수 초기화)
- `weights` 설정값은 유지됨 (리셋해도 슬라이더 값 그대로)
- 가중치 패널 다시 표시됨

---

## 13. 테마송 (BGM)

### 재생 파일

- 파일 위치: `public/tema_song.mp3` (또는 `.ogg` 등 브라우저 지원 포맷)
- Vite 기준 `/tema_song.mp3` 경로로 참조

### 재생 타이밍

| 트리거 | 동작 |
|--------|------|
| 투구! 버튼 클릭 (`idle` → `windup` 전환 시점) | 테마송 처음부터 재생 시작 |
| `reveal` 또는 `fourball` 진입 시 | 테마송 정지 |
| `fullReset` (🔄 처음부터 버튼) 시 | 테마송 정지 |

- `ball-wait` / `strike-wait` 자동 루프 중에는 테마송이 **계속 재생** (정지하지 않음)
- 재생 도중 투구가 반복되어도 처음부터 다시 시작하지 않고 **이어서 재생**

### 구현 방식

- `useRef<HTMLAudioElement>`로 `<audio>` 요소 보관
- `audio.currentTime = 0; audio.play()` — 투구 시작 시
- `audio.pause(); audio.currentTime = 0;` — 게임 종료(reveal / fourball / reset) 시
- `loop = false` (한 번 재생 후 자동 종료. 곡이 짧으면 `loop = true`로 변경 가능)
- 볼륨: `audio.volume = 0.7` (기본값)

### 브라우저 정책 참고

- 투구 버튼 클릭(사용자 인터랙션) 후 재생이므로 autoplay 정책에 저촉되지 않음
- `play()`는 Promise 반환 → `.catch(() => {})` 로 재생 실패 무시 처리

---

## 12. 기타 동작

- 투구 중(windup, pitch, name-show, strike, ball) 및 자동 진행 대기(ball-wait, strike-wait)에는 버튼 없음
- 새로고침 시 모든 설정(가중치, strikeTarget) 초기화 (localStorage 미구현)
- 모바일/데스크탑 반응형 (Tailwind md 브레이크포인트 기준)
- 텍스트 드래그 선택 불가 (`select-none`)
