services:
  app:
    image: oven/bun:1
    working_dir: /app
    volumes:
      - .:/app
      - bun_modules:/app/node_modules
    ports:
      - "8080:8080"
    environment:
      - CHOKIDAR_USEPOLLING=true
    command: sh -c "bun install && bun run dev"

volumes:
  bun_modules:
